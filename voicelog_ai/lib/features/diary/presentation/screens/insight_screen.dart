import 'package:flutter/material.dart';

/// 인사이트 탭 화면 (플레이스홀더).
class InsightScreen extends StatelessWidget {
  const InsightScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('인사이트 — 준비 중'),
    );
  }
}
